server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ncc0706/arukas-config-server-db
#          username: 
#          password:   
          search-paths:
          - app*
          force-pull: true